export const gameModeNumberToString = (gameModeNumber) => {
    switch(gameModeNumber) {
        case 0: return "Standard Story Writing"
        case 1: return "Classic Folding Story"
        case 2: return "Writing Prompt"
    }
}